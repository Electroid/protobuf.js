name: "Test"
on:
  push:
    branches:
    - master
  pull_request:
    types: [assigned, opened, synchronize, reopened, labeled]
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-node@v4
      with:
        node-version: "12"
    - uses: oven-sh/setup-bun@v2
      with:
        bun-version: "latest"
    - name: "Install dependencies"
      run: bun install
    - name: "Lint sources"
      run: bun run lint:sources -- --max-warnings 0
    - name: "Lint types"
      run: bun run lint:types
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node_version: ["12", "14", "16", "18", "20", "22"]
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node_version }}
    - uses: oven-sh/setup-bun@v2
      with:
        bun-version: "latest"
    - name: "Install dependencies"
      run: bun install
    - name: "Test sources"
      run: bun run test:sources
    - name: "Test types"
      run: bun run test:types
  bench:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-node@v4
      with:
        node-version: "12"
    - uses: oven-sh/setup-bun@v2
      with:
        bun-version: "latest"
    - name: "Install dependencies"
      run: bun install
    - name: "Run benchmark"
      run: bun run bench
